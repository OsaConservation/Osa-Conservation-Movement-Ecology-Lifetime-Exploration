# Data cleaning - Mammals

All tracking devices come with errors, and each track must be checked visual and cleaned of outliers before analysis.

This page shows this process for each mammal within the project. It has been automated so that we always have access to a stardardised and cleaned locations database. 


```{r, include=F, eval=T}
# What animals do we need to do this for
#tmp_mammal
 
# Remove Manu because it didnt work
tmp_mammal<- tmp_mammal[tmp_mammal$name!="Manu",] 
tmp_mammal$number <- 1:nrow(tmp_mammal)
i <- 1
```



## Tapirs

For the tapir collars we use the Telonics - TGW-4577-4 model, wioth data sent through the Iridium system.  

They have low voltage and mortality alerts. 

There are two types of location data these collars collect - regular GPS and QFP. QFP's are Quick Fix Positions and use less battery than a standard GPS fix. Units can be configured to collect GPS, QFP, or a mix of the two location types - we collect a mix. 

Our collars collect QFP's when there is *no GPS signal available*. 

`Succeded` = a successful GPS location
`Resolved QFP` = a successful QFP location
`Resolved QFP` = an uncertain QFP location (lower quality)
`Unresolved QFP` = A poor quality QFP 

Lets explore the implications of these different fix types for one individual - Juancito.

### Juancito (CR)

```{r m1}
################################################################################
m1 <- mov_dat[mov_dat$name=="Juancito",]
#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)


#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))


trails <- st_read("shapefiles/OC_trails2.shp")
trails_ship <- st_read("shapefiles/shipetiari_trails.shp")
trails_ship<- st_zm(trails_ship, drop = TRUE, what = "ZM")
```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from Juancito. Of those, they are of the following types. 

```{r m1a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m1b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)     

# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We can then explore how these different fix types introduce error into the locations:

*NOTE* ZOOM IN!

```{r m1c}

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)        
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 0.5,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Succeeded"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Succeeded"], 
                   color="#000", stroke=F,radius=2,fillOpacity=1,
                   popup="Succeeded") %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Resolved QFP"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Resolved QFP"], 
                   color="#1ff", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP") %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"], 
                   color="#f80", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Unresolved QFP"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Unresolved QFP"], 
                   color="#f00", stroke=F,radius=2,fillOpacity=1,
                   popup="Unresolved QFP (Uncertain)")%>% addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft") %>% 
    addLegend("topleft",
              labels = c("GPS", "Resolved QFP", "Uncertain QFP", "Unresolved QFP"),
              colors=c("black", "#1ff", "#f80", "#f00"),
            title = "Fix type",
            opacity=1)

```

So there is unreliability in all of the QFP estimates, but the GPS look great (not much spread). 

Lets explore the error around each type of fix (if it has an estimate present):

Only GPS data has an error in meters (max 127m):

```{r m1d}

boxplot(m_df$location_error_numerical~as.factor(m_df$gps_fix_type_raw))
```

QFP data has error in doplar estimates:
```{r m1e}
#boxplot(m_df$gps_hdop~as.factor(m_df$gps_fix_type_raw))
tmp <- m_df[m_df$gps_fix_type_raw!="Succeeded",]
fig <- plot_ly(tmp, y = ~gps_hdop, color = ~gps_fix_type_raw, type = "box")

fig
```

So we have a huge spread in error. Lets subset to anything with a hdop <10,and resolved. This substantially reduced the spread but still leaves a couple of issues - notable outliers. 

```{r m1f}

# Filter
filtered <- m1[m1$gps_fix_type_raw=="Succeeded" | ((m1$gps_hdop < 10) & m1$gps_fix_type_raw %in% c("Resolved QFP", "Resolved QFP (Uncertain)")),]

# Make a dataframe
filtered_df <- as.data.frame(filtered)

# move2
m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)        
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Succeeded"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Succeeded"], 
                   color="#000", stroke=F,radius=2,fillOpacity=1,
                   popup="Succeeded") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Resolved QFP"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Resolved QFP"], 
                   color="#1ff", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"], 
                   color="#f80", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Unresolved QFP"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Unresolved QFP"], 
                   color="#f00", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We should also use ctmm (as in the vultures) to find errors:

```{r m1g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[1]] <- m2[OUT$distance<8000,]
# Check the outlier is gone [Only if required]

m2 = clean_mammal[[1]]
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[1]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Tuva (CR)

```{r m2}
i <- i+1

################################################################################
m1 <- mov_dat[mov_dat$name=="Tuva",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
m_df$timestamp <- ymd_hms(m_df$timestamp)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))
```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from Tuva. Of those, they are of the following types. 

```{r m2a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Which means even less "resolved" locations.

Plot of the raw data:

```{r m2b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)       
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We can then explore how these different fix types introduce error into the locations:

*NOTE* ZOOM IN!

```{r m2c}

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)         
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 0.5,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Succeeded"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Succeeded"], 
                   color="#000", stroke=F,radius=2,fillOpacity=1,
                   popup="Succeeded") %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Resolved QFP"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Resolved QFP"], 
                   color="#1ff", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP") %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"], 
                   color="#f80", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="Unresolved QFP"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="Unresolved QFP"], 
                   color="#f00", stroke=F,radius=2,fillOpacity=1,
                   popup="Unresolved QFP (Uncertain)")%>% addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft") %>% 
    addLegend("topleft",
              labels = c("GPS", "Resolved QFP", "Uncertain QFP", "Unresolved QFP"),
              colors=c("black", "#1ff", "#f80", "#f00"),
            title = "Fix type",
            opacity=1)

```

So there is unreliability in all of the QFP estimates, but the GPS look great (not much spread). 

Lets explore the error around each type of fix (if it has an estimate present):

Only GPS data has an error in meters (max 127m):

```{r m2d}

boxplot(m_df$location_error_numerical~as.factor(m_df$gps_fix_type_raw))
```

QFP data has error in dopler estimates:

```{r m2e}
#boxplot(m_df$gps_hdop~as.factor(m_df$gps_fix_type_raw))
tmp <- m_df[m_df$gps_fix_type_raw!="Succeeded",]
fig <- plot_ly(tmp, y = ~gps_hdop, color = ~gps_fix_type_raw, type = "box")

fig
```

So we have a huge spread in error. Lets subset to anything with a hdop <10,and resolved. This substantially reduced the spread but still leaves a couple of issues. 

```{r m2f}

# Filter
filtered <- m1[m1$gps_fix_type_raw=="Succeeded" | ((m1$gps_hdop < 10) & m1$gps_fix_type_raw %in% c("Resolved QFP", "Resolved QFP (Uncertain)")),]

# Make a dataframe
filtered_df <- as.data.frame(filtered)

# move2
m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)         
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Succeeded"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Succeeded"], 
                   color="#000", stroke=F,radius=2,fillOpacity=1,
                   popup="Succeeded") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Resolved QFP"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Resolved QFP"], 
                   color="#1ff", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"], 
                   color="#f80", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Unresolved QFP"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Unresolved QFP"], 
                   color="#f00", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")%>% 
    addLegend("topleft",
              labels = c("GPS", "Resolved QFP", "Uncertain QFP", "Unresolved QFP"),
              colors=c("black", "#1ff", "#f80", "#f00"),
            title = "Fix type",
            opacity=1)
```

We should also use ctmm (as in the vultures) to find errors:

```{r m2g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=3, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[2]] <- m2[OUT$distance<3000,]
# Check the outlier is gone [Only if required]

m2 = clean_mammal[[2]]
filtered_df <- as.data.frame(clean_mammal[[2]])
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)    
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Succeeded"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Succeeded"], 
                   color="#000", stroke=F,radius=2,fillOpacity=1,
                   popup="Succeeded") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Resolved QFP"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Resolved QFP"], 
                   color="#1ff", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Resolved QFP (Uncertain)"], 
                   color="#f80", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>%
    addCircleMarkers(lng=filtered_df$location_long[filtered_df$gps_fix_type_raw=="Unresolved QFP"],
                   lat=filtered_df$location_lat[filtered_df$gps_fix_type_raw=="Unresolved QFP"], 
                   color="#f00", stroke=F,radius=2,fillOpacity=1,
                   popup="Resolved QFP (Uncertain)") %>% 
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[2]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 
 
#i <- i + 1
```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[2]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

## Jaguars

For carnivores we use Vectronic collars.

Large carnivores we have VERTEX Plus (GPS + ACC) and VERTEX Vision (GPS + ACC + Video)

### Killa (PE)

VERTEX Plus collar. 

```{r m4}
i <- i+1

################################################################################
m1 <- mov_dat[mov_dat$name=="Killa",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))
```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m4a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m4b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%     
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We can then explore how these different fix types introduce error into the locations:

*NOTE* ZOOM IN!

```{r m4c}

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%     
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 0.5,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="3D GPS Fix"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="3D GPS Fix"], 
                   color="#000", stroke=F,radius=2,fillOpacity=1,
                   popup=m_df$timestamp[m_df$gps_fix_type_raw=="3D GPS Fix"]) %>%
    addCircleMarkers(lng=m_df$location_long[m_df$gps_fix_type_raw=="3D valid GPS Fix"],
                   lat=m_df$location_lat[m_df$gps_fix_type_raw=="3D valid GPS Fix"], 
                   color="#1ff", stroke=F,radius=2,fillOpacity=1,
                   popup=m_df$timestamp[m_df$gps_fix_type_raw=="3D valid GPS Fix"]) %>%
    addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft") %>% 
    addLegend("topleft",
              labels = c("3D GPS Fix", "3D valid GPS Fix"),
              colors=c("black", "#1ff"),
            title = "Fix type",
            opacity=1)

```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r m4g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Shipetiari (PE)

VERTEX Plus collar. 

```{r m7}
i <- i+1

################################################################################
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Shipetiari",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m7a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m7b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r m7g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Matsonsori (PE)

VERTEX Plus collar. 

```{r m11}
################################################################################
i <- i+1

#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Matsonsori",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m11a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m11b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

Remove the crazy outlier. 

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r m11g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Amigo (PE)

VERTEX Plus collar. 

```{r m110}
################################################################################
i <- i+1

#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Amigo",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m110a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m110b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r m110g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Chimuelo (PE)

VERTEX Plus collar. 

```{r m111}
################################################################################
i <- i+1

#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Chimuelo",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m111a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m111b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r m111g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Ricolas (PE)

VERTEX Plus collar. 

```{r m112}
################################################################################
i <- i+1

#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Ricolas",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m112a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m112b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r m112g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Baby (PE)

VERTEX Plus collar. 

```{r mbaby}
################################################################################
i <- i+1

#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Baby",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mbabya}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mbabyb}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mbabyg}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 




## Pumas


### Tico (CR)

VERTEX Plus collar. 

```{r m5}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Tico",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))
```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m5a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m5b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r m5g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Coyunda (CR)

VERTEX Plus collar. 

```{r m6}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Coyunda",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))
```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m6a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m6b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)


```

How is she moving through time:

```{r}
plot(mt_time(m2),(mt_distance(m2)/(mt_time_lags(m2)/60))/1000, las=1,
     pch=19, cex=0.3, ylab="km per hour", xlab="")
```




No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r m6g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Kirari (PE)

VERTEX Plus collar. 

```{r m8}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Kirari",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m8a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m8b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r m8g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Valentina (CR)

VERTEX Plus collar. 

```{r mValentina}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Valentina",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mValentinaa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mValentinab}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r mValentinag}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Kosni (PE)

VERTEX Plus collar. 

```{r mKosni}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Kosni",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mKosnia}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mKosnib}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r mKosnig}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Macho2 (CR)

VERTEX Plus collar. 

```{r mMacho2}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Macho2",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mMacho2a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mMacho2b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r mMacho2g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Maximo (PE)

VERTEX Plus collar. 

```{r mMaximo2}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Maximo",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mMaximoa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mMaximob}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```

We get doppler error estimates on these data, and they are all fairly small!:

```{r}
boxplot(m_df$gps_dop~m_df$gps_fix_type_raw, las=1)
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

No cleaning done to date:

```{r}
# Filter
filtered <- m2
# Make a dataframe
filtered_df <- as.data.frame(filtered)

```


We should also use ctmm (as in the vultures) to find errors:

```{r mMaximog}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 

clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

# m2 = mt_as_move2(clean_mammal[[i]], track_id_columna = m1$name[1])
# dat_lines <- m2 %>%
#   mt_track_lines()
# m <- leaflet() %>%
#   # Add a satellite image layer
#   addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
#   addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
#   addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# # Add the shapefiles
# m %>% addPolylines(data = dat_lines,
#                    # Colour by individual
#                    #color = dat_lines$colours,
#                    # The line properties
#                    weight = 1,#opacity = 0.6,
#                    # hover label
#                    label = dat_lines$local_identifier) %>%
#   # Add a legend
#   addLayersControl(
#     baseGroups = c("OS", "Simple", "Satellite")) %>%
#   addScaleBar(position = "topleft")
# 
# # Recreate the telemtry object
# m_df <- as.data.frame(clean_mammal[[i]])
# tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage, mode = 'line') %>% 
  layout(
    yaxis = list(
      range=c(2500,3500)
    )
  )  

p

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Suxo (PE)

VERTEX Plus collar. 

```{r mSuxo2, eval=F}
################################################################################

m1 <- mov_dat[mov_dat$name=="Suxo",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

Only one data point to date. 



## Ocelots

### Kurá (CR)

VERTEX Plus collar. 

```{r m9}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Kurá",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m9a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m9b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

Remove the crazy outlier. 

```{r}
# Filter
filtered <- m2[m2$location_lat>6,]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r m9g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#filtered <- filtered[OUT$speed<0.25,]
clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Jerry (CR)

VERTEX Plus collar. 

```{r mJerry}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Jerry",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mJerrya}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mJerryb}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2[!(m2$gps_fix_type_raw%in% c("Unresolved QFP", "Resolved QFP (Uncertain)")),]
table(m2$gps_fix_type_raw)

# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mJerryg}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
#      ylab="Minimum speed (m/s)",
#      xlab="Median deviation (km)", ylim=c(0,5))

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed<0.5,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Echo (CR)

VERTEX Plus collar. 

```{r mEcho}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Echo",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mEchoa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mEchob}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}

# Filter
filtered <- m2#[m2$gps_fix_type_raw!="Unresolved QFP",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mEchog}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[(OUT$distance/1000)<25,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Emilia (CR)

VERTEX Plus collar. 

```{r mEmilia}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Emilia",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mEmiliaa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mEmiliab}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2[m2$gps_fix_type_raw!="Unresolved QFP",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mEmiliag}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
filtered <- filtered[OUT$speed < 0.3,]

# tel = as.telemetry(as.data.frame(filtered))
# #Import: we can add the estimation of error for Shakira from above code 
# # However, the error is negligible, but as an example is useful
# uere(tel)-> UERE
# 
# # Check outliers
# par(mfrow = c(1, 2))
# outlie(tel)-> OUT
# #plot(OUT, units = FALSE)
# plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
#      ylab="Minimum speed (m/s)",
#      xlab="Median deviation (km)")
# 
# # SPECIES SPECIFIC THRESHOLD
# abline(v=8, lty=2, col="red")
# abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#filtered <- filtered[OUT$speed < 1,]





#plot(filtered)
clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")
 
# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Orianna (CR)

VERTEX Plus collar. 

```{r mOrianna}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Orianna",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mOriannaa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mOriannab}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2[m2$gps_fix_type_raw!="Unresolved QFP",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mOriannag}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")


# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed<0.5,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Maathai (CR)

VERTEX Plus collar. 

```{r mMaathai}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Maathai",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mMaathaia}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mMaathaib}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
table(m2$gps_fix_type_raw)
filtered <- m2[m2$gps_fix_type_raw!="Unresolved QFP",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mMaathaig}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed<1,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Katsímari (PE)

VERTEX Plus collar. 

```{r m10}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Katsímari",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r m10a}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r m10b}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```

Remove the crazy outlier. 

```{r}
# Filter
filtered <- m2[m2$location_long<(-65),]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r m10g}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed<0.5,]
# Check the outlier is gone [Only if required]

# # Create a telemtry object
# tel = as.telemetry(as.data.frame(clean_mammal[[i]]))
# #Import: we can add the estimation of error for Shakira from above code 
# # However, the error is negligible, but as an example is useful
# uere(tel)-> UERE
# 
# # Check outliers
# par(mfrow = c(1, 2))
# outlie(tel)-> OUT
# #plot(OUT, units = FALSE)
# plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
#      ylab="Minimum speed (m/s)",
#      xlab="Median deviation (km)")
# 
# # SPECIES SPECIFIC THRESHOLD
# abline(v=8, lty=2, col="red")
# abline(h=0.5, lty=2, col="red")



# tel = as.telemetry(as.data.frame((clean_mammal[[i]])))
# #Import: we can add the estimation of error for Shakira from above code 
# # However, the error is negligible, but as an example is useful
# uere(tel)-> UERE
# 
# # Check outliers
# par(mfrow = c(1, 2))
# outlie(tel)-> OUT
# #plot(OUT, units = FALSE)
# plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
#      ylab="Minimum speed (m/s)",
#      xlab="Median deviation (km)")
# 
# # SPECIES SPECIFIC THRESHOLD
# abline(v=8, lty=2, col="red")
# abline(h=0.5, lty=2, col="red")





m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 


```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Pasooki (PE)

VERTEX Plus collar. 

```{r mPasooki}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Pasooki",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mPasookia}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mPasookib}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
table(m2$gps_fix_type_raw)

filtered <- m2[m2$gps_fix_type_raw!="Unresolved QFP",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mPasookig}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed<1,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Tunche (PE)

VERTEX Plus collar. No data yet.  

```{r mTunche}
# ################################################################################
# i <- i+1
# #tmp_mammal$name
# m1 <- mov_dat[mov_dat$name=="Tunche",]
# 
# #head(m1)
# 
# # Make a dataframe
# m_df <- as.data.frame(m1)
# #head(m_df)
# tmp <- data.frame(table(m_df$gps_fix_type_raw))

```


### Cecina (PE)

VERTEX Plus collar. 

```{r mCecina}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Cecina",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mCecinaa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mCecinab}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mCecinag}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed<0.5,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Ranga (PE)

VERTEX Plus collar. 

```{r mRanga}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Ranga",]

# Seemed to die around 2024-08-31
#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#m_df$timestamp

#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types.

THIS INDIVIDUAL SEEMED TO DIE AROUND 2024-08-31

```{r mRangaa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mRangab}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])


m2 <- m2[m2$timestamp< ymd_hms("2024-08-31 12:00:00"),]
m_df <- as.data.frame(m2)

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mRangag}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Cocona (PE)

VERTEX Plus collar. 

```{r mCocona}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Cocona",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mCoconaa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mCoconaab}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mCoconag}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


### Namu (CR)

VERTEX Plus collar. 

```{r mNamu}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Namu",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mNamua}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mNamub}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mNamug}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed< 0.5,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Chullachaqui (PE)

VERTEX Plus collar. 

```{r mChull}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Chullachaqui",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))
```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mChulla}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mChullb}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mChullg}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
tmp <- filtered[OUT$speed< 0.5,]
# Check the outlier is gone [Only if required]
# We still have one outlier
min(tmp$location_long)

clean_mammal[[i]]<- tmp[tmp$location_long != min(tmp$location_long), ]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```


Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 


## White-lipped Peccaries

### Sini (PE)

VERTEX Plus collar. 

```{r mSini}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Sini",]
#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]


# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mSinia}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mSinib}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1) %>% #opacity = 0.6,
                   # hover label
                   #label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1,
                   popup=m_df$timestamp) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>%
      addMarkers(lng=-83.355923	,
                   lat=8.428206,
                   popup="Capture Corral") %>%
  
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mSinig}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered[OUT$speed< 1,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage) %>%
  add_lines(name = 'Tag voltage') %>%
  add_segments(
    x = min(filtered_df$timestamp), xend = max(filtered_df$timestamp),
    y = 3000, yend = 3000,
    line = list(color = 'red', dash = 'dash'),
    name = 'Critical threshold'
  ) %>%
  layout(
    yaxis = list(
      range = c(2500, 3500)
    )
  )
p


```

### Maicito (PE)

VERTEX Plus collar. 

```{r mMaicito}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Maicito",]
#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]


# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mMaicitoa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mMaicitob}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1) %>% #opacity = 0.6,
                   # hover label
                   #label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1,
                   popup=m_df$timestamp) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>%
  
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mMaicitog}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered#[OUT$speed< 1,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage) %>%
  add_lines(name = 'Tag voltage') %>%
  add_segments(
    x = min(filtered_df$timestamp), xend = max(filtered_df$timestamp),
    y = 3000, yend = 3000,
    line = list(color = 'red', dash = 'dash'),
    name = 'Critical threshold'
  ) %>%
  layout(
    yaxis = list(
      range = c(2500, 3500)
    )
  )
p


```

### Shuya (PE)

VERTEX Plus collar. 

```{r mShuyra}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Shuyra",]
#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]


# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mShuyaa}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mShuyab}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1) %>% #opacity = 0.6,
                   # hover label
                   #label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1,
                   popup=m_df$timestamp) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>%
  
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2#[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mShuyag}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered   #[OUT$speed< 1,]
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

**Current battery level:**

Be worried if it goes below 3000!

```{r}
p <- plot_ly(filtered_df, x = ~timestamp, y = ~tag_voltage) %>%
  add_lines(name = 'Tag voltage') %>%
  add_segments(
    x = min(filtered_df$timestamp), xend = max(filtered_df$timestamp),
    y = 3000, yend = 3000,
    line = list(color = 'red', dash = 'dash'),
    name = 'Critical threshold'
  ) %>%
  layout(
    yaxis = list(
      range = c(2500, 3500)
    )
  )
p


```


## Short eared dogs

### Otinkami (PE)

VERTEX Plus collar. 

```{r mOtinkami}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Otinkami",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mOtinkamia}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mOtinkamib}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") 

#%>% 
# Add shapefile for trails
#addPolylines(data = trails, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2[m2$gps_fix_type_raw=="Succeeded",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mOtinkamig}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Otsiti (PE)

VERTEX Plus collar. 

```{r mOtsiti}
################################################################################
i <- i+1
#tmp_mammal$name
m1 <- mov_dat[mov_dat$name=="Otsiti",]

#head(m1) 
m1$taxon_cannonical_name <- tmp_mammal$taxon_canonical_name[tmp_mammal$name==m1$name[1]] 
m1$country <- tmp_mammal$country[tmp_mammal$name==m1$name[1]]

# Make a dataframe
m_df <- as.data.frame(m1)
#head(m_df)
tmp <- data.frame(table(m_df$gps_fix_type_raw))

```

First observation: `r first(m_df$timestamp)`

Last observation:  `r last(m_df$timestamp)`

Active for: `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`

To date we have `r nrow(m_df)` fixes from `r print(m1$name[1])`. Of those, they are of the following types. 

```{r mOtsitia}
tmp$Percentage <- round(tmp$Freq/nrow(m_df),2)
tmp %>%
  kbl(row.names = FALSE) %>%
  kable_styling() 
```

Plot of the raw data:

```{r mOtsitib}
# Height above elipsoid check
# summary(m_df$gps_satellite_count)
# summary(m_df$height_above_ellipsoid)
# plot(m_df$location_long, m_df$location_lat)
# test <- m_df[m_df$height_above_ellipsoid>5000 & is.na(m_df$height_above_ellipsoid)==F ,]
# points(test$location_long, test$location_lat, col="red")

# move2
m2 = mt_as_move2(m1, track_id_columna = m1$name[1])

# Convert to track
dat_lines <- m2 %>% 
  mt_track_lines()

m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") %>% 
# Add shapefile for trails
addPolylines(data = trails_ship, color = "black", weight=2,opacity=1)    
  
# Add the shapefiles 
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   color = "#FF10F0",
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$local_identifier) %>%
    addCircleMarkers(lng=m_df$location_long,
                   lat=m_df$location_lat, 
                   color= "#FF10F0", radius=1) %>%
    addCircleMarkers(lng=m_df$location_long[1],
                   lat=m_df$location_lat[1], 
                   color="#03F",
                   popup="start") %>% 
    addCircleMarkers(lng=m_df$location_long[nrow(m_df)],
                   lat=m_df$location_lat[nrow(m_df)], 
                   color="red",
                   popup="end") %>% 
  
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>% 
  addScaleBar(position = "topleft")
```


The average number of fixes per day to date **`r round(nrow(m_df)/as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1)`** per day.

Those are distributed through time as follows:

```{r}
par(mfrow=c(1,1))
tmp2 <- m_df
tmp2$day <- substr(m_df$timestamp,1,10)
tmp2$day <- factor(tmp2$day, levels=as.character(seq(as.Date(first(tmp2$day)), as.Date(last(tmp2$day)), by="days")))

tmp2 <- tmp2 %>% group_by(day, .drop=FALSE) %>% summarise(fixes=n())

barplot(tmp2$fixes, names.arg=tmp2$day, las=2,
        cex.names=0.7)
```


Remove the crazy outliers. 

```{r}
# Filter
filtered <- m2[m2$gps_fix_type_raw!="Unresolved QFP",]
# Make a dataframe
filtered_df <- as.data.frame(filtered)

#m2 = mt_as_move2(filtered, track_id_columna = m1$name[1])

```


We should also use ctmm (as in the vultures) to find errors:

```{r mOtsitig}
# Finding outliers
# There are several ways to remove them but I'm going to use atlastools and ctmm framework
# METHOD 1 - GEOFENCE
#plot(m_df$location_long, m_df$location_lat, asp = 1, type = "o")
#m_df <- atl_filter_bounds(filtered_df, x = "location_long", 
#                              y = "location_lat", x_range = c(-88, -60), 
#                              y_range = c(-20, 20),remove_inside = FALSE)

# Method 2 -
# Create a telemtry object
tel = as.telemetry(filtered_df)
#Import: we can add the estimation of error for Shakira from above code 
# However, the error is negligible, but as an example is useful
uere(tel)-> UERE

# Check outliers
par(mfrow = c(1, 2))
outlie(tel)-> OUT
#plot(OUT, units = FALSE)
plot(OUT$distance/1000, OUT$speed, pch=19,col=rgb(0,0,0,0.2),
     ylab="Minimum speed (m/s)",
     xlab="Median deviation (km)")

# SPECIES SPECIFIC THRESHOLD
abline(v=8, lty=2, col="red")
abline(h=0.5, lty=2, col="red")

# Delete extreme values 
#plot(filtered)
clean_mammal[[i]] <- filtered
# Check the outlier is gone [Only if required]

m2 = mt_as_move2(clean_mammal[[i]], track_id_column = "track", time_column= "timestamp")
dat_lines <- m2 %>%
  mt_track_lines()
m <- leaflet() %>%
  # Add a satellite image layer
  addProviderTiles(providers$CartoDB.PositronNoLabels, group="Simple") %>%
  addProviderTiles(providers$Esri.WorldImagery       , group="Satellite") %>%
  addProviderTiles(providers$OpenStreetMap.Mapnik    , group="OS") #%>%
# Add the shapefiles
m %>% addPolylines(data = dat_lines,
                   # Colour by individual
                   #color = dat_lines$colours,
                   # The line properties
                   weight = 1,#opacity = 0.6,
                   # hover label
                   label = dat_lines$track) %>%
  # Add a legend
  addLayersControl(
    baseGroups = c("OS", "Simple", "Satellite")) %>%
  addScaleBar(position = "topleft")

# Recreate the telemtry object
m_df <- as.data.frame(clean_mammal[[i]])
tel <- as.telemetry(m_df)

# Check range-residency (i.e home range)
svf = variogram(tel, res = 1)
level <- c(0.5, 0.95) # 50% and 95% CIs
xlim <- c(0,24 %#% "hours") # 0-12 hour window
plot(svf, fraction = 0.65, level = level, main = m1$name[1]) 

```

 
Distance moved: `r round(as.numeric(sum(mt_distance(clean_mammal[[i]]), na.rm=T))/1000,1)` km in `r paste(round(as.numeric(last(m_df$timestamp) - first(m_df$timestamp)),1), "days")`. 

### Upload clean data

```{r mammal_upload}
# Create.csv
mammal_stack <- mt_stack(clean_mammal)
tmp_csv <- as.data.frame(mammal_stack)
tmp_csv$geometry <- NULL
write.csv(tmp_csv, "tmp/mammal_clean.csv", row.names=F)
saveRDS(mammal_stack, "tmp/mammal_stack.RDS")
 
#OC googledrive
drive_update(media = 'tmp/mammal_clean.csv',file=as_id("1HoyWJFhpFRyF_CYj_YStFu-BCXgBTP_T"))
drive_update(media = 'tmp/mammal_stack.RDS',file=as_id("13jUPdZjhT3ua0CHUCvaQvant12c6Zf2y"))

#Chris' googledrive
#drive_update(media = 'tmp/mammal_clean.csv',file=as_id("1J5cE3CFitFbUUg1wA0GLYvONEcybSi56"))
#drive_update(media = 'tmp/mammal_stack.RDS',file=as_id("15KbO5FCX_VUNJKN53XX0TehtLtJntX9d"))


```



